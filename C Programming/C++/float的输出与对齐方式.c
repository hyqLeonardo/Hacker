#include <stdio.h>
main()
{
	float f,c;
	int lower,upper,step;
	lower=0;
	upper=300;
	step=20;
	f=lower;
	while(f<=upper)
	{
		c=(5.0/9.0)*(f-32.0);
 	    printf("%3.0f %6.2f\n",f,c); //输出时默认是右对齐，但因为小数位的个数的强制性（优先级）大于总位数（6.2的.2比6更强制），所以，
		f=f+step;                    //如果当小数位占用了总位数之后，小数点前面的整数部分没有手动分配的位数显示时，就会自动向后推进
	}                                //把6.2换成5.2就能看出来，此时还是右对齐，不过自动补充的位数把格式向右推进了！
}
